-- Plotter:
    -- keep the same amount of axis numbers regardless of the current scale (render decimal value when scale is small and ignore some values when it's big);
    -- think over the method of accepting the user input;
    -- bug: zooming in stops working at some point;
    -- cursor-dependant zooming (not necessary).
-- Parser:
    -- "2e^x" is invalid for some reason;
    -- maybe using trees for expressions is too inefficient, what about stack-based parsing (shunting-yard algorithm)?
    -- add parameters;
    -- add comments to the code.

To be mentioned in README:
    -- recursive descent parsing and grammar;
    -- polymorphic code in C: how and why it's useful;
    -- the decision to give up on using arena allocators;
    -- partial unity builds and Makefiles;
    -- X-macros in tests.
