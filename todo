-- support factors in front of variables and functions (e.g. 2x);
-- support removing allocated objects in arena.c (maybe in separate branch?);
-- handle errors in arena.c;
-- `1-1` bug;
-- handle division by zero (done?).
